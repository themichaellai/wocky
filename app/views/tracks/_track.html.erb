<%= track.name %> - <%= track.artist %>
<%= track.vote_count %>
<%= form_for([track, @vote]) do |f| %>
  <%= f.hidden_field :ip, value: request.env['REMOTE_ADDR'] %>
  <%= f.submit '+', class: 'btn-link' %>
<% end %>
<br />
